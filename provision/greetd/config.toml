# greetd configuration for Pallet OS
# See: man greetd.conf

[terminal]
vt = 1

[default_session]
command = "/usr/local/bin/pallet-session"
user = "pallet"

[initial_session]
command = "/usr/local/bin/pallet-session"
user = "pallet"
